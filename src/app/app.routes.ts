import { Routes } from '@angular/router';
import { HarViewerComponent } from './features/har-viewer/har-viewer.component';

export const routes: Routes = [{ path: '', component: HarViewerComponent }];
